{
  "name": "@impressdesigns/static-assets",
  "version": "0.0.0",
  "private": true,
  "description": "Static brand assets for Impress Designs — source SVGs built into production assets.",
  "type": "module",
  "scripts": {
    "build": "npm run build:assets && npm run build:site",
    "build:assets": "python3 scripts/build.py",
    "build:site": "eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6",
    "sharp": "^0.35.1",
    "svgo": "^4.0.1"
  }
}
