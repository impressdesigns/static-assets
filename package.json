{
  "name": "@impressdesigns/static-assets",
  "version": "0.0.0",
  "private": true,
  "description": "Static brand assets for Impress Designs — source SVGs built into production assets.",
  "type": "module",
  "scripts": {
    "build": "python3 scripts/build.py"
  },
  "devDependencies": {
    "sharp": "^0.35.1",
    "svgo": "^4.0.1"
  }
}
